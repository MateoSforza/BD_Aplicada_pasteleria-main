# CARPETA HOOKS - FUNCIONES Y ARCHIVOS

## ¿QUÉ ES ESTA CARPETA?
La carpeta `hooks/` contiene React hooks personalizados que encapsulan lógica reutilizable para el manejo de estado y efectos.

## ARCHIVOS Y SUS FUNCIONES:

### useAuth.ts
- **Función**: Hook para manejo de autenticación
- **Qué hace**: 
  - Gestiona el estado de autenticación del usuario
  - Proporciona funciones para login y logout
  - Mantiene la sesión en localStorage
  - Verifica si el usuario está autenticado
- **Funciones exportadas**:
  - `login(credentials)`: Inicia sesión
  - `logout()`: Cierra sesión
  - `isAuthenticated()`: Verifica si está autenticado
  - `user`: Objeto del usuario actual
  - `loading`: Estado de carga
  - `error`: Errores de autenticación

### useApi.ts
- **Función**: Hook genérico para peticiones a la API
- **Qué hace**: 
  - Maneja estados de loading, error y datos
  - Proporciona función para recargar datos
  - Gestiona el ciclo de vida de peticiones HTTP
- **Funciones exportadas**:
  - `data`: Datos obtenidos de la API
  - `loading`: Estado de carga
  - `error`: Error si ocurre
  - `refetch()`: Función para recargar datos

### useTortas.ts
- **Función**: Hook específico para datos de tortas
- **Qué hace**: 
  - Usa `useApi` internamente
  - Carga datos de tortas desde la API
  - Proporciona estado específico para tortas
- **Funciones exportadas**:
  - `tortas`: Array de tortas
  - `loading`: Estado de carga
  - `error`: Error si ocurre
  - `refetchTortas()`: Recargar tortas

### useMedidas.ts
- **Función**: Hook específico para datos de medidas
- **Qué hace**: 
  - Carga datos de medidas desde la API
  - Permite filtrar medidas por torta
  - Maneja estado específico de medidas
- **Funciones exportadas**:
  - `medidas`: Array de medidas
  - `loading`: Estado de carga
  - `error`: Error si ocurre
  - `getMedidasByTorta(tortaId)`: Filtrar por torta

### useIngredientes.ts
- **Función**: Hook específico para datos de ingredientes
- **Qué hace**: 
  - Carga datos de ingredientes desde la API
  - Maneja estado específico de ingredientes
- **Funciones exportadas**:
  - `ingredientes`: Array de ingredientes
  - `loading`: Estado de carga
  - `error`: Error si ocurre

### useCostosExtra.ts
- **Función**: Hook específico para datos de costos extra
- **Qué hace**: 
  - Carga datos de costos extra desde la API
  - Maneja estado específico de costos extra
- **Funciones exportadas**:
  - `costosExtra`: Array de costos extra
  - `loading`: Estado de carga
  - `error`: Error si ocurre

## PATRÓN DE DISEÑO:
- Hooks personalizados siguen el patrón `use...`
- Encapsulan lógica compleja y la hacen reutilizable
- Manejan estados comunes: data, loading, error
- Abstraen las llamadas a la API

## ESTRUCTURA TÍPICA DE UN HOOK:
```typescript
export const useData = () => {
  const [data, setData] = useState([]);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<string | null>(null);

  useEffect(() => {
    // Lógica para cargar datos
  }, []);

  return { data, loading, error, refetch };
};
```

## CÓMO SE USAN:
```typescript
import { useAuth } from '../hooks/useAuth';
import { useTortas } from '../hooks/useTortas';

// En un componente
const { user, login, logout } = useAuth();
const { tortas, loading, error } = useTortas();
```

## BENEFICIOS:
- **Reutilización**: Lógica compartida entre componentes
- **Separación**: Lógica separada de la presentación
- **Testing**: Fácil de testear de forma aislada
- **Mantenimiento**: Cambios centralizados
- **Legibilidad**: Componentes más limpios

## HOOKS DE REACT USADOS:
- `useState`: Manejo de estado local
- `useEffect`: Efectos secundarios
- `useCallback`: Memoización de funciones
- `useMemo`: Memoización de valores

## ESTADO ACTUAL:
- ✅ **useAuth**: Completamente funcional
- 🔄 **useApi y hooks específicos**: En desarrollo
- 📋 **Hooks avanzados**: Pendientes (filtros, búsqueda, paginación)