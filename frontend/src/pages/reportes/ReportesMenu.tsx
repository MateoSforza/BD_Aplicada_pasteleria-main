import React from 'react'

// ReportesMenu ahora está vacío porque el layout principal (`ReportesLayout`)
// renderiza las tarjetas visuales del panel principal. Dejamos este componente
// como placeholder para la ruta index por compatibilidad.
const ReportesMenu: React.FC = () => {
  return null
}

export default ReportesMenu
