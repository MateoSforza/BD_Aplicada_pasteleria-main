# CARPETA TYPES - FUNCIONES Y ARCHIVOS

## ¿QUÉ ES ESTA CARPETA?
La carpeta `types/` contiene todas las definiciones de tipos TypeScript que se usan en toda la aplicación. Proporciona tipado fuerte y ayuda con el IntelliSense.

## ARCHIVOS Y SUS FUNCIONES:

### auth.ts
- **Función**: Tipos relacionados con autenticación
- **Tipos definidos**:
  ```typescript
  interface Usuario {
    id: number;
    nombreUsuario: string;
    // otros campos del usuario
  }

  interface LoginCredentials {
    username: string;
    password: string;
  }

  interface LoginResponse {
    message: string;
    success: boolean;
    user?: Usuario;
  }
  ```
- **Uso**: Login, registro, gestión de sesiones
- **Estado**: ✅ COMPLETADO

### tortas.ts
- **Función**: Tipos relacionados con tortas
- **Tipos definidos**:
  ```typescript
  interface Torta {
    id: number;
    nombre: string;
    descripcion?: string;
    medidas: Medida[];
  }
  ```
- **Uso**: Páginas de tortas, componentes relacionados
- **Estado**: 🔄 EN DESARROLLO

### medidas.ts
- **Función**: Tipos relacionados con medidas
- **Tipos definidos**:
  ```typescript
  interface Medida {
    id: number;
    tortaId: number;
    tamaño: string;
    precio: number;
    // otros campos
  }

  interface MedidaDetalle {
    id: number;
    medida: Medida;
    ingredientes: IngredienteMedida[];
    costosExtra: CostoExtraMedida[];
  }
  ```
- **Uso**: Páginas de medidas, drill-down, detalles
- **Estado**: 🔄 EN DESARROLLO

### ingredientes.ts
- **Función**: Tipos relacionados con ingredientes
- **Tipos definidos**:
  ```typescript
  interface Ingrediente {
    id: number;
    nombre: string;
    precio: number;
    stock: number;
    unidadMedida: string;
    proveedor?: string;
  }

  interface IngredienteMedida {
    ingredienteId: number;
    ingrediente: Ingrediente;
    cantidad: number;
    costo: number;
  }
  ```
- **Uso**: Gestión de ingredientes, cálculos de costos
- **Estado**: 🔄 EN DESARROLLO

### costosExtra.ts
- **Función**: Tipos relacionados con costos extra
- **Tipos definidos**:
  ```typescript
  interface CostoExtra {
    id: number;
    nombre: string;
    tipo: 'FIJO' | 'VARIABLE';
    valor: number;
  }

  interface CostoExtraMedida {
    costoExtraId: number;
    costoExtra: CostoExtra;
    valor: number;
  }
  ```
- **Uso**: Gestión de costos adicionales
- **Estado**: 🔄 EN DESARROLLO

### reportes.ts
- **Función**: Tipos para reportes y análisis
- **Tipos definidos**:
  ```typescript
  interface ReporteVentas {
    periodo: string;
    tortasMasVendidas: TortaVenta[];
    ingresoTotal: number;
  }

  interface ReporteIngredientes {
    ingrediente: Ingrediente;
    stockActual: number;
    stockMinimo: number;
    estado: 'SUFICIENTE' | 'BAJO' | 'CRITICO';
  }
  ```
- **Uso**: Páginas de reportes, análisis
- **Estado**: 📋 PENDIENTE

### api.ts
- **Función**: Tipos para respuestas de API
- **Tipos definidos**:
  ```typescript
  interface ApiResponse<T> {
    data: T;
    success: boolean;
    message?: string;
  }

  interface ApiError {
    message: string;
    code: number;
    details?: any;
  }
  ```
- **Uso**: Tipado de respuestas HTTP
- **Estado**: 🔄 EN DESARROLLO

### common.ts
- **Función**: Tipos comunes y utilitarios
- **Tipos definidos**:
  ```typescript
  interface PaginatedResponse<T> {
    items: T[];
    totalCount: number;
    pageSize: number;
    currentPage: number;
  }

  type LoadingState = 'idle' | 'loading' | 'success' | 'error';
  ```
- **Uso**: Tipos reutilizables en toda la app
- **Estado**: 🔄 EN DESARROLLO

## VENTAJAS DEL TIPADO FUERTE:
- **IntelliSense**: Autocompletado en el editor
- **Detección de errores**: Errores en tiempo de compilación
- **Refactoring seguro**: Cambios sin romper código
- **Documentación**: Los tipos documentan la estructura
- **Mantenibilidad**: Código más predecible

## PATRÓN DE NOMENCLATURA:
- **Interfaces**: PascalCase (ej: `Usuario`, `LoginResponse`)
- **Types**: PascalCase para aliases complejos
- **Enums**: UPPER_CASE para valores (ej: `ESTADO.ACTIVO`)

## CÓMO SE USAN:
```typescript
import { Usuario, LoginCredentials } from '../types/auth';
import { Torta } from '../types/tortas';

// En funciones
const login = async (credentials: LoginCredentials): Promise<Usuario> => {
  // implementación
};

// En componentes
interface Props {
  torta: Torta;
  onSelect: (id: number) => void;
}
```

## MEJORES PRÁCTICAS:
- Un archivo por dominio/entidad
- Interfaces para objetos, types para uniones
- Tipos específicos mejor que `any`
- Reutilizar tipos comunes
- Mantener sincronizado con el backend

## ESTADO ACTUAL:
- ✅ **auth.ts**: Completamente definido
- 🔄 **Tipos de entidades**: En desarrollo
- 📋 **Tipos avanzados**: Pendientes (reportes, filtros)

## SINCRONIZACIÓN CON BACKEND:
- Los tipos deben coincidir con los DTOs del backend
- Actualizar tipos cuando cambie la API
- Usar herramientas como OpenAPI para generar tipos automáticamente