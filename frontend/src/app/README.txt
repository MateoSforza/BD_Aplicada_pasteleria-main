# Carpeta app

Contiene la configuración global de la aplicación React.

- App.tsx: layout principal, navbar y outlet de rutas.
- queryClient.ts: configuración de React Query para manejo de caché y queries.
- routes.tsx: definición de rutas principales de la app (dashboard, ingredientes, etc.).
